def city_functions(city_name, country_name, population):
    # receive city name and a country name
    # one line combo
    city_country_combo = city_name + ', ' + country_name + ' population - ' + str(population)
    # sending back data
    return city_country_combo